import { Outlet } from 'react-router-dom';

const Layout = () => {
  return (
    <div>
      {/* <NavigationBar /> */}
      11
      <div>
        <Outlet />
      </div>
    </div>
  );
};

export default Layout;
