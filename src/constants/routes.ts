export const ROUTES = {
  home: '/',
  detail: '/:characterId',
  character: '/character',
  union: '/union',
  guild: '/guild',
  statistics: '/statistics',
  rank: '/rank',
} as const;

export default ROUTES;
